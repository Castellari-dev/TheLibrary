<Window x:Class="TheLibrary.Views.ArtPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Escolher arte / edição"
        Height="720" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Bg}"
        Foreground="{DynamicResource Text}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Line}"
                BorderThickness="0,0,0,1" Padding="18,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TxtSearch" Grid.Column="0" KeyDown="TxtSearch_KeyDown" />
                <Button Grid.Column="1" Content="Buscar" Width="110" Margin="10,0,0,0" Click="Search_Click" />
                <CheckBox Grid.Column="2" x:Name="ChkMultilingual" Content="Outros idiomas"
                          Margin="16,0,0,0" VerticalAlignment="Center" Click="Search_Click" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Padding="18,10,18,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="TxtInfo" Style="{StaticResource Muted}" />
                <ProgressBar x:Name="Progress" Width="160" Margin="14,0,0,0" IsIndeterminate="True"
                             Visibility="Collapsed" />
            </StackPanel>
        </Border>

        <ListBox x:Name="LstArts" Grid.Row="2" Margin="12" BorderThickness="0" Background="Transparent"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 MouseDoubleClick="LstArts_MouseDoubleClick">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="196" Margin="6">
                        <Border CornerRadius="9" ClipToBounds="True" Background="{DynamicResource SurfaceAlt}">
                            <Image Source="{Binding NormalImage}" Width="184" Height="257" Stretch="Uniform" />
                        </Border>
                        <TextBlock Text="{Binding SetLine}" TextWrapping="Wrap" FontSize="11"
                                   Margin="2,7,2,0" TextAlignment="Center" />
                        <TextBlock Text="{Binding PriceLine}" FontSize="11" Margin="2,3,2,0"
                                   TextAlignment="Center" Foreground="{DynamicResource TextMuted}" />
                        <TextBlock Text="{Binding EffectiveArtist}" FontSize="10" Margin="2,2,2,0"
                                   TextAlignment="Center" Foreground="{DynamicResource TextMuted}"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="3" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Line}"
                BorderThickness="0,1,0,0" Padding="18,12">
            <Grid>
                <TextBlock x:Name="TxtSelected" Style="{StaticResource Muted}" HorizontalAlignment="Left"
                           Text="{Binding SelectedItem.SetLine, ElementName=LstArts}" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancelar" Style="{StaticResource GhostButton}" Width="110" Click="Cancel_Click" />
                    <Button Content="Usar esta arte" Width="150" Margin="10,0,0,0" Click="Confirm_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
